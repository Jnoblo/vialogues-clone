Vialogues Clone — V2 collaborative (Replit-ready)

Nouvelles fonctionnalités :
- Récupérer le timecode depuis le lecteur
- Réponses imbriquées aux annotations
- Catégories d'annotation
- Code unique de projet (PRJ-XXXXXX)
- Notifications par mail aux membres du projet (configurable)

Demo admin:
- email: demo@vialogues.local
- password: admin123

SMTP configuration (optionnel):
- set the following secrets in Replit or env:
  SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM (optional)

Run:
npm install
npm start
Open at http://localhost:4000 or Replit URL.
